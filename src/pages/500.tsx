import { Layout } from '@/layout/layout';
import { ReactElement } from 'react';

const Error500 = (): ReactElement => (
  <Layout>
    <h1>Puts, aconteceu algo inesperado, os devs serão avisados. F pelo problema</h1>
  </Layout>
);
export default Error500;
