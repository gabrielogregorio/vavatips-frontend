/* eslint-disable no-unused-vars */
import React from 'react';

export type buttonTypeNormal = {
  children: any;
  disabled?: boolean;
  className?: string;
  onClick?: React.MouseEventHandler<HTMLButtonElement>;
  ariaLabel?: string;
};
